---
phase: 03-socket-api-session-persistence
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/cmux-cli
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements:
  - SOCK-03

must_haves:
  truths:
    - "A user can run `scripts/cmux-cli` to send commands to the running cmux Linux app over the socket"
    - "SOCK-03 is marked complete in REQUIREMENTS.md"
  artifacts:
    - path: "scripts/cmux-cli"
      provides: "Executable wrapper script invoking tests_v2/cmux.py as CLI"
      contains: "cmux.py"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirement status"
      contains: "[x] **SOCK-03**"
  key_links:
    - from: "scripts/cmux-cli"
      to: "tests_v2/cmux.py"
      via: "exec python3"
      pattern: "tests_v2/cmux.py"
---

<objective>
Close SOCK-03 gap: provide a Linux CLI entry point for controlling cmux over the socket.

Purpose: The verification found cmux.py works as a library but there is no standalone CLI.
Per D-04, the Python client IS the Phase 3 CLI. This plan creates a wrapper script that
makes cmux.py invocable as a command, and marks SOCK-03 complete.

Output: Executable `scripts/cmux-cli` wrapper, updated REQUIREMENTS.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-socket-api-session-persistence/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cmux-cli wrapper script and mark SOCK-03 complete</name>
  <files>scripts/cmux-cli, .planning/REQUIREMENTS.md</files>
  <read_first>
    - tests_v2/cmux.py (lines 1093-1113 — the main() CLI entry point)
    - scripts/ (list directory to see existing script patterns)
    - .planning/REQUIREMENTS.md (current SOCK-03 line)
  </read_first>
  <action>
1. Create `scripts/cmux-cli` — a bash wrapper script that:
   - Sets PYTHONPATH to include the repo's `tests_v2/` directory
   - Resolves the repo root relative to the script location (using dirname/realpath)
   - Exec's `python3 tests_v2/cmux.py "$@"` passing all arguments through
   - Has a shebang `#!/usr/bin/env bash`
   - Is chmod +x

2. Update `.planning/REQUIREMENTS.md`:
   - Change `- [ ] **SOCK-03**:` to `- [x] **SOCK-03**:`
   - In the Traceability table, change SOCK-03 status from `Pending` to `Complete`

Per D-04: "The 'cmux CLI' for Phase 3 is the Python client tests_v2/cmux.py.
A native Rust CLI binary comes in Phase 5."
  </action>
  <verify>
    <automated>test -x scripts/cmux-cli && grep -q "cmux.py" scripts/cmux-cli && grep -q "\[x\] \*\*SOCK-03\*\*" .planning/REQUIREMENTS.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/cmux-cli` exists, is executable, and contains reference to `tests_v2/cmux.py`
    - `scripts/cmux-cli --help` prints argparse help (exit 0) when cmux.py is importable
    - `.planning/REQUIREMENTS.md` contains `[x] **SOCK-03**`
    - Traceability table shows SOCK-03 as Complete
  </acceptance_criteria>
  <done>SOCK-03 gap closed: Linux users can run scripts/cmux-cli to control the app via socket</done>
</task>

</tasks>

<verification>
- `scripts/cmux-cli` is executable and invokes cmux.py correctly
- REQUIREMENTS.md reflects SOCK-03 as complete
- No other files modified (surgical change)
</verification>

<success_criteria>
- scripts/cmux-cli exists and is executable
- SOCK-03 marked complete in REQUIREMENTS.md
- Wrapper script correctly resolves paths to tests_v2/cmux.py
</success_criteria>

<output>
After completion, create `.planning/phases/03-socket-api-session-persistence/03-07-SUMMARY.md`
</output>
