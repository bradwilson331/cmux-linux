---
phase: 01-ghostty-foundation
plan: 05
type: execute
wave: 5
depends_on: [01-04]
files_modified: [build.rs, src/main.rs, stubs.c]
autonomous: true
requirements: [GHOST-02]
user_setup: []
gap_closure: true

must_haves:
  truths:
    - "cargo build succeeds without undefined symbol errors"
    - "App binary links with libghostty.a correctly"
    - "All ghostty FFI symbols resolve at link time"
  artifacts:
    - path: "build.rs"
      provides: "Correct linkage directives for binary crate"
      contains: "cargo:rustc-link"
    - path: "stubs.c"
      provides: "Missing function stubs"
      contains: "ghostty_surface_draw"
  key_links:
    - from: "build.rs"
      to: "libghostty.a"
      via: "cargo:rustc-link directives"
      pattern: "cargo:rustc-link-lib=static=ghostty"
---

<objective>
Fix linking errors preventing the application from building and running

Purpose: Close verification gap where app fails to link due to missing libghostty.a symbols
Output: Successfully linked binary that can launch
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ghostty-foundation/01-VERIFICATION.md
@.planning/phases/01-ghostty-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix build.rs linkage propagation</name>
  <files>build.rs</files>
  <action>
    Fix the build.rs to ensure libghostty.a links to the final binary:
    - Remove the incorrect lib.rs approach (per D-01 from CONTEXT.md: single binary crate)
    - Move all cargo:rustc-link directives to the main build.rs
    - Ensure the ghostty/zig-out/lib path is properly specified
    - Add println! directives that apply to the binary target
    
    The issue is that the current build.rs tries to use a lib+bin crate structure, but the
    linkage directives only apply to the lib crate, not propagating to the binary.
    
    Reference from 01-04-SUMMARY: lib.rs was created for testing but breaks linkage.
    Gap reason: Library build gets -l static=ghostty but binary build doesn't inherit it.
  </action>
  <verify>
    <automated>cargo clean && cargo build 2>&1 | grep -v "undefined reference"</automated>
  </verify>
  <done>build.rs properly configures linkage for binary crate</done>
</task>

<task type="auto">
  <name>Task 2: Add missing Ghostty function stubs</name>
  <files>stubs.c</files>
  <action>
    Add stub implementations for the missing ghostty functions:
    - ghostty_surface_draw: Called from GtkGLArea render callback
    - ghostty_surface_refresh: May be called during surface updates
    
    These should be minimal stubs that allow linking. The real implementations
    exist in libghostty.a but aren't being found due to the linkage issue.
    If they truly don't exist in libghostty.a, provide working stubs.
    
    Gap reason: Missing ghostty_surface_draw and ghostty_surface_refresh stubs
  </action>
  <verify>
    <automated>gcc -c stubs.c -o /tmp/test_stubs.o 2>&1 | grep -c "error:" | grep "^0$"</automated>
  </verify>
  <done>stubs.c compiles without errors and provides missing symbols</done>
</task>

<task type="auto">
  <name>Task 3: Verify successful build and launch</name>
  <files>src/main.rs</files>
  <action>
    After fixing linkage and stubs, verify the app builds and runs:
    - Run cargo build to ensure successful compilation
    - Check that the binary exists at target/debug/cmux-linux
    - Try to run the binary (may fail at runtime but should not have link errors)
    - If it runs, verify basic GTK window appears
    
    No code changes needed in main.rs for this task - just verification.
  </action>
  <verify>
    <automated>cargo build && test -f target/debug/cmux-linux && echo "Binary built successfully"</automated>
  </verify>
  <done>Application binary builds without linking errors</done>
</task>

</tasks>

<verification>
After completion:
- cargo build succeeds without undefined symbol errors
- Binary at target/debug/cmux-linux exists
- ldd on binary shows libghostty symbols resolved
</verification>

<success_criteria>
- Build completes successfully without linking errors
- All ghostty FFI symbols resolve
- Binary can be executed (even if it fails at runtime)
</success_criteria>

<output>
After completion, create `.planning/phases/01-ghostty-foundation/01-05-SUMMARY.md`
</output>