---
phase: 01-ghostty-foundation
plan: 09
type: execute
wave: 3
depends_on: [01-08]
files_modified: [src/ghostty/mod.rs, src/ghostty/surface.rs, src/ghostty/callbacks.rs]
autonomous: false
gap_closure: true
requirements: [GHOST-03, GHOST-04, GHOST-05]
must_haves:
  truths:
    - "User can type and see characters appear"
    - "User can copy and paste text"
    - "Terminal responds to keyboard input with < 10ms latency"
  artifacts:
    - path: "src/ghostty/surface.rs"
      provides: "Complete input and clipboard handling"
  key_links:
    - from: "keyboard controller"
      to: "ghostty_surface_key"
      via: "key event handlers"
    - from: "clipboard callbacks"
      to: "GTK clipboard API"
      via: "gdk::Display clipboard"
---

<objective>
Verify and fix terminal input, output, and clipboard functionality

Purpose: Achieve full terminal interactivity per Phase 1 requirements
Output: Working terminal with typing and clipboard support
</objective>

<context>
@.planning/phases/01-ghostty-foundation/01-08-SUMMARY.md
@src/ghostty/surface.rs
@src/ghostty/input.rs
@src/ghostty/callbacks.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Ghostty library integration</name>
  <files>src/ghostty/mod.rs, src/ghostty/surface.rs</files>
  <action>
    Check if libghostty.a is properly linked and functions are available:
    
    1. Add a test module that verifies key ghostty functions exist:
       - ghostty_init
       - ghostty_app_new
       - ghostty_surface_new
       - ghostty_surface_key
       - ghostty_surface_draw
    
    2. Use nm or objdump to verify symbols in libghostty.a:
       nm ghostty/zig-out/lib/libghostty.a | grep ghostty_surface_draw
    
    3. If symbols missing, investigate if:
       - Zig build needs different flags
       - We need to implement these in our Zig fork
       - Functions have different names than expected
    
    4. Add explicit extern declarations for any missing functions
    
    5. Consider if we need to call ghostty_surface_show or similar to make content visible
  </action>
  <verify>nm ghostty/zig-out/lib/libghostty.a | grep -c ghostty_surface</verify>
  <done>Ghostty library symbols verified and properly linked</done>
</task>

<task type="auto">
  <name>Task 2: Test and fix input pipeline</name>
  <files>src/ghostty/surface.rs</files>
  <action>
    Ensure keyboard input reaches the terminal:
    
    1. Add logging in key_pressed handler to verify:
       - Keycodes are received
       - Correct ghostty key codes are mapped
       - ghostty_surface_key is called
       - No errors occur
    
    2. Test with simple keys like 'a', 'Enter', 'Backspace'
    
    3. Verify focus is set on the surface:
       - Call ghostty_surface_set_focus(surface, true) after creation
       - Set can_focus(true) on GLArea
       - Request focus on window show
    
    4. Check if we need to call ghostty_surface_tick or similar to process input
    
    5. Verify PTY is properly initialized and shell is running
  </action>
  <verify>cargo run 2>&1 | grep -E "key_pressed|keycode"</verify>
  <done>Keyboard input pipeline verified and working</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 1 terminal with input and rendering</what-built>
  <how-to-verify>
    1. Build and run: cargo build --release && cargo run --release
    2. When window appears, click inside to focus
    3. Type: echo "Hello from Ghostty"
    4. Press Enter
    5. Select some text with mouse
    6. Press Ctrl+Shift+C to copy
    7. Press Ctrl+Shift+V to paste
    
    Expected results:
    - Characters appear as you type
    - Shell prompt is visible
    - Commands execute when you press Enter
    - Text selection works with mouse
    - Copy/paste works
  </how-to-verify>
  <resume-signal>Type "working" if terminal is fully functional, or describe what's not working</resume-signal>
</task>

</tasks>

<verification>
Full terminal functionality: typing, shell interaction, clipboard
</verification>

<success_criteria>
- Terminal displays shell prompt
- Typing shows characters immediately (< 10ms latency)
- Commands can be executed
- Copy/paste works on X11 and Wayland
- No crashes during normal use
</success_criteria>

<output>
After completion, create `.planning/phases/01-ghostty-foundation/01-09-SUMMARY.md`
</output>