---
phase: 01-ghostty-foundation
plan: 08
type: execute
wave: 2
depends_on: [01-07]
files_modified: [src/ghostty/surface.rs, stubs.c]
autonomous: false
gap_closure: true
requirements: [GHOST-02]
must_haves:
  truths:
    - "Terminal renders visible content"
    - "User can see the shell prompt"
  artifacts:
    - path: "src/ghostty/surface.rs"
      provides: "Working render pipeline"
    - path: "stubs.c"
      provides: "Missing function implementations"
  key_links:
    - from: "GtkGLArea::render signal"
      to: "ghostty_surface_draw"
      via: "connect_render callback"
---

<objective>
Implement missing Ghostty functions and fix rendering pipeline

Purpose: Make the terminal actually display content
Output: Visible terminal with shell prompt
</objective>

<context>
@.planning/phases/01-ghostty-foundation/01-07-SUMMARY.md
@src/ghostty/surface.rs
@stubs.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing ghostty function stubs</name>
  <files>stubs.c</files>
  <action>
    The verification report shows ghostty_surface_draw and ghostty_surface_refresh are undefined.
    Add stub implementations to stubs.c:
    
    ```c
    // Ghostty surface rendering stubs
    void ghostty_surface_draw(void* surface) {
        // This should trigger OpenGL drawing but for now just stub it
        // The real implementation would be in libghostty.a
    }
    
    void ghostty_surface_refresh(void* surface) {
        // This should refresh/invalidate the surface
        // The real implementation would be in libghostty.a  
    }
    ```
    
    Then rebuild stubs.o:
    gcc -c stubs.c -o stubs.o
    
    Note: These are temporary stubs. The real fix is to ensure libghostty.a
    exports these symbols, but this gets us past the linking error.
  </action>
  <verify>gcc -c stubs.c -o stubs.o && cargo build</verify>
  <done>stubs.o rebuilt with missing functions, app links successfully</done>
</task>

<task type="auto">
  <name>Task 2: Debug render pipeline and fix GL context issues</name>
  <files>src/ghostty/surface.rs</files>
  <action>
    Add comprehensive debugging to understand why rendering isn't working:
    
    1. In realize callback:
       - Log GL version, renderer info
       - Verify make_current() succeeds
       - Check GL error state after ghostty calls
    
    2. In render callback:
       - Add glClearColor + glClear as fallback if ghostty_surface_draw does nothing
       - Log each render call
       - Return proper Propagation value
    
    3. Add queue_render() call after surface creation to trigger initial draw
    
    4. Ensure wakeup_cb is properly triggering redraws via queue_render()
    
    This will help diagnose if the issue is:
    - GL context not ready
    - Ghostty not initialized properly  
    - Render callbacks not being invoked
    - Or libghostty.a not actually implementing the functions
  </action>
  <verify>cargo run 2>&1 | grep -E "render|GL|draw"</verify>
  <done>Render pipeline instrumented with debugging, GL state validated</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Terminal window with Ghostty surface initialization</what-built>
  <how-to-verify>
    1. Run: cargo build && cargo run
    2. Look for the application window
    3. Check if you see:
       - A window appears (even if black/empty)
       - No segfault or crash
       - Debug output shows initialization sequence
    4. If terminal content visible, try typing a character
  </how-to-verify>
  <resume-signal>Type "visible" if you see terminal content, "black" if window is black, or describe any errors</resume-signal>
</task>

</tasks>

<verification>
Terminal window should appear without crashing
</verification>

<success_criteria>
- App runs without segfault
- Window appears and stays open
- Debug logs show render pipeline executing
</success_criteria>

<output>
After completion, create `.planning/phases/01-ghostty-foundation/01-08-SUMMARY.md`
</output>