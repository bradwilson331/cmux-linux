---
phase: 07.1-ssh-workspace-ui
plan: 01
subsystem: ui
tags: [gtk4, ssh, toml, autocomplete, keyboard-shortcuts]

# Dependency graph
requires:
  - phase: 07-ssh-remote-workspaces
    provides: SSH lifecycle, bridge, tunnel backend
provides:
  - SSH hosts module with config parsing and persistence
  - GTK4 SSH connect dialog with autocomplete
  - Ctrl+Shift+S shortcut binding for SSH workspace creation
  - Auto-save of successfully connected hosts
  - Hostname-based workspace auto-naming
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "GTK4 modal dialog with EntryCompletion for autocomplete (#[allow(deprecated)])"
    - "Atomic file write pattern (write .tmp then rename) for ssh_hosts.toml"
    - "SSH config Host-line parser (simple line-by-line, no external crate)"

key-files:
  created:
    - src/ssh_hosts.rs
    - src/ssh_dialog.rs
  modified:
    - src/config.rs
    - src/shortcuts.rs
    - src/main.rs
    - src/workspace.rs

key-decisions:
  - "EntryCompletion used despite GTK 4.10 deprecation -- no replacement exists until GTK 5"
  - "Workspace auto-naming extracts hostname only (strips user@ and :port)"

patterns-established:
  - "Modal dialog pattern: gtk4::Window with modal+transient_for, EventControllerKey for Escape, connect_destroy for focus restore"

requirements-completed: [SSH-01]

# Metrics
duration: 5min
completed: 2026-03-27
---

# Phase 7.1 Plan 01: SSH Workspace UI Summary

**Ctrl+Shift+S SSH connect dialog with autocomplete from saved hosts and ~/.ssh/config, hostname-based workspace naming, and auto-save on successful connection**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-27T02:53:35Z
- **Completed:** 2026-03-27T02:59:00Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- SSH hosts module with config parsing, persistence, and workspace name extraction (12 unit tests)
- GTK4 SSH connect dialog with Entry, EntryCompletion, Escape/Enter handling, focus restore
- Configurable Ctrl+Shift+S shortcut wired through existing ShortcutAction/ShortcutMap system
- Auto-save of connected hosts to ~/.config/cmux/ssh_hosts.toml with atomic writes

## Task Commits

Each task was committed atomically:

1. **Task 1: SSH hosts module** - `3140e0b0` (feat)
2. **Task 2: SSH connect dialog** - `6cfc3a9d` (feat)

## Files Created/Modified
- `src/ssh_hosts.rs` - SSH config parsing, saved hosts TOML persistence, workspace name extraction, 12 unit tests
- `src/ssh_dialog.rs` - GTK4 modal dialog with Entry + EntryCompletion autocomplete, SSH workspace creation
- `src/config.rs` - NewSshWorkspace variant in ShortcutAction enum, config field, KNOWN_SHORTCUTS, default binding
- `src/shortcuts.rs` - Match arm for NewSshWorkspace, handler function calling show_ssh_dialog
- `src/main.rs` - Module declarations, APP_CSS dialog styles, auto-save host on Connected state
- `src/workspace.rs` - Workspace::new_remote uses workspace_name_from_target for hostname-based naming

## Decisions Made
- Used EntryCompletion despite GTK 4.10 deprecation -- no replacement exists, works in GTK 4.12, won't be removed until GTK 5
- Workspace auto-naming extracts hostname only (strips user@ prefix and :port suffix) per D-08

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- SSH workspace UI is complete and ready for manual UAT
- Full flow: Ctrl+Shift+S -> dialog -> type host -> Enter -> SSH workspace created
- Depends on Phase 7 SSH backend being functional for end-to-end testing

---
*Phase: 07.1-ssh-workspace-ui*
*Completed: 2026-03-27*
