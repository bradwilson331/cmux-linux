---
phase: 7.1
slug: ssh-workspace-ui
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-26
---

# Phase 7.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | cargo test (Rust) + pytest (Python socket tests) |
| **Config file** | `Cargo.toml` / `tests_v2/` |
| **Quick run command** | `cargo build` |
| **Full suite command** | `cargo build && cargo test` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cargo build`
- **After every plan wave:** Run `cargo build && cargo test`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 7.1-01-01 | 01 | 1 | SSH-01 | build | `cargo build` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

*Existing infrastructure covers all phase requirements.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Ctrl+Shift+S opens SSH dialog | SSH-01 | GTK dialog requires running app | Launch app, press Ctrl+Shift+S, verify dialog appears |
| Autocomplete shows SSH config hosts | SSH-01 | Requires ~/.ssh/config and running app | Type partial hostname, verify dropdown appears |
| Host saved after successful connection | SSH-01 | Requires real SSH server | Connect to host, check ~/.config/cmux/ssh_hosts.toml |
| Workspace auto-named from hostname | SSH-01 | Requires running app + SSH | Connect, verify sidebar shows hostname |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
