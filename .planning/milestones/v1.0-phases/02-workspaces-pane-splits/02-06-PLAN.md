---
phase: 02-workspaces-pane-splits
plan: 06
type: execute
wave: 5
depends_on:
  - 02-05
files_modified: []
autonomous: false
requirements:
  - WS-01
  - WS-02
  - WS-03
  - WS-04
  - WS-05
  - WS-06
  - SPLIT-01
  - SPLIT-02
  - SPLIT-03
  - SPLIT-04
  - SPLIT-05
  - SPLIT-06
  - SPLIT-07
must_haves:
  truths:
    - "User can create, switch, rename, and close workspaces via keyboard and click"
    - "User can split panes and resize dividers with drag"
    - "Keyboard input goes to the active pane after every focus change"
    - "No process::exit when a shell exits in a pane"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint for the complete Phase 2 multiplexer UI. The user runs the app and exercises all Phase 2 features to confirm they work correctly before the phase is marked complete.

Purpose: Automated compilation verification cannot confirm correct visual rendering, focus routing after splits, drag-to-resize behavior, or the close-workspace confirmation dialog. This checkpoint catches functional issues before Phase 3 builds on top.

Output: User confirmation that all Phase 2 success criteria pass, or a description of failures that trigger gap-closure plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-workspaces-pane-splits/02-05-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 2 multiplexer UI:
    - Sidebar workspace list (160px, dark background, active workspace highlighted)
    - Workspace create (Ctrl+N), close (Ctrl+Shift+W with dialog), switch (click / Ctrl+] / Ctrl+[ / Ctrl+1–9), rename (Ctrl+Shift+R inline entry)
    - Pane split right (Ctrl+D), split down (Ctrl+Shift+D), close pane (Ctrl+Shift+X)
    - Pane focus navigation (Ctrl+Alt+arrows)
    - Drag-to-resize dividers (GtkPaned native)
    - Sidebar toggle (Ctrl+B)
    - Active pane indicator (1px blue border on focused pane)
  </what-built>
  <how-to-verify>
    Build and run the app first:
    ```bash
    cd /home/twilson/code/cmux-linux
    cargo build 2>&1 | tail -5
    ./target/debug/cmux
    ```

    Exercise each Phase 2 feature in order:

    **1. Initial state**
    - Window opens at 800x600
    - Sidebar visible on the left (160px, dark #242424 background)
    - Sidebar shows "Workspace 1" with blue (#5b8dd9) active highlight
    - Terminal pane fills the right side with a 1px blue border (active-pane indicator)

    **2. Workspace create (WS-01)**
    - Press Ctrl+N
    - Sidebar adds "Workspace 2" row, which becomes the active (blue) row
    - A new terminal pane appears on the right
    - Type `echo hello` in the new workspace — it works

    **3. Workspace switch (WS-03)**
    - Click on "Workspace 1" in the sidebar — terminal switches back to workspace 1
    - Press Ctrl+N again to create "Workspace 3"
    - Press Ctrl+[ and Ctrl+] to cycle workspaces (wrap-around at ends)
    - Press Ctrl+1 to jump directly to workspace 1

    **4. Workspace rename (WS-04)**
    - Switch to workspace 2
    - Press Ctrl+Shift+R
    - Sidebar row shows a text entry (placeholder "Workspace name")
    - Type "Dev" and press Enter
    - Sidebar row now shows "Dev"
    - Press Ctrl+Shift+R again, type "Test", press Escape
    - Sidebar row still shows "Dev" (Escape cancelled)

    **5. Pane split right (SPLIT-01)**
    - Switch to a workspace
    - Press Ctrl+D
    - Pane splits into two side-by-side panes (50/50 ratio)
    - New right pane has the blue active-pane border
    - Type in the new pane — keystrokes appear in the correct (right) pane

    **6. Pane split down (SPLIT-02)**
    - In the right pane, press Ctrl+Shift+D
    - Right pane splits into top-right and bottom-right panes
    - New bottom-right pane has the blue border and receives keyboard input

    **7. Drag-to-resize (SPLIT-04)**
    - Drag the vertical divider between panes left and right
    - Panes resize fluidly without custom code

    **8. Pane focus navigation (SPLIT-03)**
    - Press Ctrl+Alt+Left — focus moves to the adjacent left pane (blue border moves)
    - Type in the newly focused pane — keystrokes go to the correct pane
    - Press Ctrl+Alt+Right — focus returns to right pane

    **9. Close active pane (SPLIT-05)**
    - With 3 panes open, press Ctrl+Shift+X
    - Active pane closes, surviving sibling expands to fill space
    - Focus moves to the surviving pane (has blue border, receives keyboard input)

    **10. Sidebar toggle (D-04)**
    - Press Ctrl+B — sidebar hides, terminal expands full width
    - Press Ctrl+B again — sidebar reappears

    **11. Close workspace with confirmation (WS-02)**
    - With 2+ workspaces, press Ctrl+Shift+W
    - Dialog appears: title "Close Workspace?", message "All panes in this workspace will be closed. This cannot be undone."
    - Click "Keep Workspace" — dialog closes, workspace stays
    - Press Ctrl+Shift+W again, click "Close Workspace" — workspace closes, other workspace becomes active

    **12. Memory sanity (no crash)**
    - Create 5 workspaces, close 4 of them
    - Split the remaining workspace 3 times, close all split panes
    - App does not crash or hang

    Report any failures with a description of what went wrong and what step it failed at.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe which steps failed (e.g., "step 5 failed: split right crashes with SIGSEGV")</resume-signal>
</task>

</tasks>

<verification>
Human confirms all 12 verification steps pass.
</verification>

<success_criteria>
All Phase 2 requirements (WS-01 through WS-06, SPLIT-01 through SPLIT-07) are observable by the user and working correctly. The phase is ready to hand off to Phase 3 (Socket API + Session Persistence).
</success_criteria>

<output>
After the checkpoint passes, create `.planning/phases/02-workspaces-pane-splits/02-06-SUMMARY.md`

Then update ROADMAP.md Phase 2 status to "Complete" and update `.planning/STATE.md` to set `stopped_at: Phase 2 complete, ready for Phase 3`.
</output>
