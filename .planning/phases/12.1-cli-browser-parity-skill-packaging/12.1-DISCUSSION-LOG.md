# Phase 12.1: CLI Browser Parity & Skill Packaging - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-03-29
**Phase:** 12.1-cli-browser-parity-skill-packaging
**Areas discussed:** CLI syntax restructure, Skill install & discovery, Surface handle design, Package integration

---

## CLI Syntax Restructure

| Option | Description | Selected |
|--------|-------------|----------|
| Nested subcommand | Add `Browser` variant with sub-enum. Old flat commands become hidden aliases. | ✓ |
| Alias layer only | Keep flat internal commands, add dispatch layer. | |
| Replace flat entirely | Remove old commands. Breaking change. | |

**User's choice:** Nested subcommand
**Notes:** Clean clap nesting preferred.

| Option | Description | Selected |
|--------|-------------|----------|
| cmux-app required | Browser commands route through socket to running cmux-app. | ✓ |
| Standalone fallback | Spawn agent-browser directly if no cmux-app. | |

**User's choice:** cmux-app required

| Option | Description | Selected |
|--------|-------------|----------|
| Human-readable default, --json for structured | Matches existing CLI pattern. | |
| JSON default for browser subcommand | Browser commands primarily used by AI agents. | ✓ |

**User's choice:** JSON default for browser subcommand
**Notes:** Opposite to the rest of the CLI, but agents are the primary consumers.

| Option | Description | Selected |
|--------|-------------|----------|
| Positional with surface: prefix | First arg parsed as surface target if matches pattern. | ✓ |
| Named flag --surface | Explicit but more verbose. | |

**User's choice:** Positional with surface: prefix

---

## Skill Install & Discovery

| Option | Description | Selected |
|--------|-------------|----------|
| cmux + cmux-browser only | Core end-user skills. Keep package lean. | ✓ |
| All cmux-* skills | Broader coverage, includes macOS-specific. | |
| All skills including release | Maximum coverage, includes internal tooling. | |

**User's choice:** cmux + cmux-browser only

| Option | Description | Selected |
|--------|-------------|----------|
| Generated CLAUDE.md at install path | Static file at /usr/share/cmux/. Claude Code auto-discovers. | ✓ |
| .claude/settings.json template | Users copy to ~/.claude/. Requires manual action. | |
| Environment variable | Profile.d script sets CMUX_SKILLS_PATH. | |

**User's choice:** Generated CLAUDE.md at install path

| Option | Description | Selected |
|--------|-------------|----------|
| No — cmux skills wrap agent-browser | Avoids duplicate interfaces. | ✓ |
| Yes — install both | Let agents choose which interface. | |

**User's choice:** No — cmux skills wrap agent-browser

| Option | Description | Selected |
|--------|-------------|----------|
| Static, baked into package | Simple, predictable. Updated on upgrade. | ✓ |
| Generated on first run | More flexible but adds startup complexity. | |

**User's choice:** Static, baked into package

---

## Surface Handle Design

| Option | Description | Selected |
|--------|-------------|----------|
| Sequential counter per session | surface:1, surface:2, etc. Resets on restart. | |
| Use existing pane/surface UUIDs | Full UUID as surface ID. | ✓ |
| Both — short ref maps to UUID | Short ref registry: surface:N -> UUID. | |

**User's choice:** Use existing pane/surface UUIDs
**Notes:** Follow-up clarified that both short refs AND UUIDs should be accepted (D-05).

| Option | Description | Selected |
|--------|-------------|----------|
| Both — short refs map to UUIDs | CLI accepts surface:N and UUIDs. | ✓ |
| UUIDs only | Skills updated to use UUIDs. | |

**User's choice:** Both — short refs map to UUIDs
**Notes:** Short refs for convenience, UUIDs as canonical. Registry in cmux-app memory.

| Option | Description | Selected |
|--------|-------------|----------|
| Caller's workspace via CMUX_WORKSPACE_ID | Skill docs specify this. | ✓ |
| Current focused workspace | Wherever focus is. | |
| New dedicated workspace | Fresh workspace for browser. | |

**User's choice:** Caller's workspace via CMUX_WORKSPACE_ID

| Option | Description | Selected |
|--------|-------------|----------|
| Error with clear message | JSON error with available surfaces list. | ✓ |
| Silent no-op | Return success with empty result. | |

**User's choice:** Error with clear message

| Option | Description | Selected |
|--------|-------------|----------|
| Identical format | Same element refs, same tree structure as agent-browser. | ✓ |
| Simplified format | Fewer attributes, different from agent-browser. | |

**User's choice:** Identical format

| Option | Description | Selected |
|--------|-------------|----------|
| In cmux-app memory | HashMap<u32, UUID>. Lost on restart. | ✓ |
| On-disk file | Written to /tmp. Survives restart. | |
| In socket response only | No persistent registry. Agent caches. | |

**User's choice:** In cmux-app memory

| Option | Description | Selected |
|--------|-------------|----------|
| Table with ref, UUID, URL, status | Essential for agent discovery. | ✓ |
| Minimal — refs and URLs | Simpler output. | |

**User's choice:** Table with ref, UUID, URL, status

| Option | Description | Selected |
|--------|-------------|----------|
| Route through socket command | JSON-RPC to cmux-app, delegates to BrowserManager. | ✓ |
| Direct agent-browser communication | Bypass cmux-app. | |

**User's choice:** Route through socket command

| Option | Description | Selected |
|--------|-------------|----------|
| Full wait support | --selector, --text, --url-contains, --load-state, --function, --timeout-ms. | ✓ |
| Defer wait to later | Ship core commands first. | |

**User's choice:** Full wait support

---

## Package Integration

| Option | Description | Selected |
|--------|-------------|----------|
| Same package | Add to existing cmux .deb/.rpm. | ✓ |
| Separate cmux-skills package | Own package with Depends: cmux. | |

**User's choice:** Same package

| Option | Description | Selected |
|--------|-------------|----------|
| /usr/share/cmux/CLAUDE.md | Next to skills directory. | ✓ |
| /etc/cmux/CLAUDE.md | System config location. | |
| ~/.claude/CLAUDE.md on first run | Runtime generation. | |

**User's choice:** /usr/share/cmux/CLAUDE.md

| Option | Description | Selected |
|--------|-------------|----------|
| Skill paths + socket API summary | Enough for Claude Code discovery. | ✓ |
| Full skill content inlined | Larger file, no separate reads. | |
| Minimal pointer only | Just paths, agents may not explore. | |

**User's choice:** Skill paths + socket API summary

---

## Claude's Discretion

- Exact clap enum structure and attribute macros
- Short ref counter implementation details
- Wait command polling mechanism
- CLAUDE.md exact prose and formatting
- Packaging script modification approach

## Deferred Ideas

None — discussion stayed within phase scope.
