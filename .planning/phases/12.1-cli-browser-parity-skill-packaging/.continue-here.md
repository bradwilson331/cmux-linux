---
phase: 12.1-cli-browser-parity-skill-packaging
task: complete
total_tasks: 6
status: complete_pending_testing
last_updated: 2026-03-31T14:43:27.977Z
---

<current_state>
Phase 12.1 execution complete. All 3 plans (6 tasks) done and verified. Post-execution bug fixes applied from real-world testing on AMD/Ubuntu 24 and Ubuntu 25.10 VM. Deb rebuilt with all fixes. Awaiting final testing on target systems before moving to Phase 13.
</current_state>

<completed_work>

- Plan 12.1-01: Nested BrowserCommand CLI with 23 variants, surface:N targeting, JSON-default output — Done
- Plan 12.1-02: Surface handle registry in AppState, socket dispatch with ref resolution — Done
- Plan 12.1-03: Skill packaging to /usr/share/cmux/skills/, CLAUDE.md in packages — Done
- Post-exec fix: Removed legacy flat Browser* variants (startup crash on Ubuntu 25.10)
- Post-exec fix: Translated CLI→agent-browser action names (open→navigate, eval→evaluate, etc.)
- Post-exec fix: Auto-enable CDP streaming on browser.open (blank preview pane)
- Post-exec fix: Map eval `expression` param to `script` for agent-browser
- Post-exec fix: Auto-prepend https:// to URLs without scheme
- Post-exec fix: Baseline x86-64 CPU target (AVX-512 SIGILL on AMD Zen 2 + VMs)
- Post-exec fix: Dynamic simdutf.o/libhighway.a discovery (eliminated stale AVX-512 objects)
- Post-exec fix: cmux-app wrapper script for GDK_BACKEND=x11 (NVIDIA GL context failure)
- README rewritten for Linux port
</completed_work>

<remaining_work>

- Test deb on AMD/Ubuntu 24 system (NVIDIA RTX 3060)
- Test deb on Ubuntu 25.10 VM (QEMU CPU)
- Clean up vm-build branch (now redundant)
- Phase 13: Portable Formats (AppImage + Flatpak) — not started
</remaining_work>

<decisions_made>

- Removed legacy Browser* variants — no backward compat needed on Linux, caused clap startup crash
- Baseline x86-64 CPU for all builds — AVX-512 only on Intel Ice Lake+ / AMD Zen 4+, too narrow
- cmux-app wrapper script — GTK4 auto-selects Wayland/EGL even in X11 sessions, breaks NVIDIA
- Dynamic zig-cache discovery — hardcoded hashes go stale on rebuild, linked old AVX-512 objects
- Auto-streaming on browser.open — users expect to see the page in the preview pane immediately
</decisions_made>

<blockers>
None — all blockers resolved in this session.
</blockers>

<context>
Big session: ran Phase 12.1 end-to-end (research→plan→verify→execute→verify), then spent significant time on real-world testing bugs. The pattern was: build deb → test on target system → hit bug → fix → rebuild. Key insight: the build machine's CPU features leak into binaries via both Rust (target-cpu) and Zig (native CPU, simdutf.o). Both needed baseline targeting. NVIDIA + GTK4 + Wayland libs on X11 is another common failure mode that needed a wrapper script.
</context>

<next_action>
Start with: Deploy dist/cmux_0.1.0_amd64.deb to AMD/Ubuntu 24 and VM systems for final validation. If working, /gsd:discuss-phase 13 for AppImage + Flatpak.
</next_action>
