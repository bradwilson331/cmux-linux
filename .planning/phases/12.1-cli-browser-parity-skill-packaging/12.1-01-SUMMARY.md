---
phase: 12.1-cli-browser-parity-skill-packaging
plan: 01
subsystem: cli
tags: [clap, json-rpc, browser-automation, cli-subcommands]

requires:
  - phase: 12-native-packages-deb-rpm
    provides: working cmux CLI binary with flat browser commands
provides:
  - BrowserCommand sub-enum with 23 action variants for nested browser subcommand syntax
  - browser_command_to_rpc dispatcher with surface_ref in JSON-RPC params
  - --no-json flag for browser commands (JSON-default output)
  - format_browser_list human-readable table formatter
  - Hidden backward-compat aliases for all legacy flat Browser* commands
affects: [12.1-02-socket-browser-dispatch, 12.1-03-skill-packaging]

tech-stack:
  added: []
  patterns: [nested-subcommand-enum, json-default-output, surface-ref-targeting]

key-files:
  created: []
  modified:
    - src/cli/mod.rs
    - src/cli/format.rs

key-decisions:
  - "Browser commands default to JSON output (--no-json to disable) per agent workflow needs"
  - "Named BrowserType variant (not Type) to avoid clap conflict with existing debug Type command"
  - "Wait command gets extended socket timeout (timeout_ms + 5s) to avoid premature disconnect"

patterns-established:
  - "Nested subcommand pattern: Commands::Browser(BrowserCommand) with separate dispatch fn"
  - "Surface ref targeting: surface_ref field in JSON-RPC params for browser surface commands"

requirements-completed: [SC-01, SC-02, SC-03, CLI-01, CLI-02, CLI-03]

duration: 3min
completed: 2026-03-30
---

# Phase 12.1 Plan 01: CLI Browser Subcommand Restructure Summary

**Nested `cmux browser <action>` subcommand hierarchy with surface ref targeting, JSON-default output, and backward-compatible hidden aliases for all legacy flat commands**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-30T04:09:34Z
- **Completed:** 2026-03-30T04:12:48Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Added BrowserCommand enum with 23 action variants matching skill documentation syntax
- All browser subcommands include surface_ref field for surface:N targeting in JSON-RPC params
- Legacy flat Browser* commands (BrowserOpen, BrowserClick, etc.) hidden but still functional
- Browser commands default to JSON output; --no-json flag switches to human-readable
- Browser list formatter renders REF/UUID/URL/STATUS table

## Task Commits

Each task was committed atomically:

1. **Task 1: Add BrowserCommand enum and restructure CLI** - `8e829d52` (feat)
2. **Task 2: Add browser list formatter to format.rs** - `828fd43e` (feat)

## Files Created/Modified
- `src/cli/mod.rs` - BrowserCommand enum, Browser variant in Commands, hidden legacy aliases, browser_command_to_rpc dispatch, --no-json flag, wait timeout handling
- `src/cli/format.rs` - format_browser_list function, browser.list dispatch in format_response

## Decisions Made
- Browser commands default to JSON output (--no-json to disable) -- agents consume JSON, humans use --no-json
- BrowserType variant named to avoid clap conflict with existing debug Type command
- Wait command socket timeout = timeout_ms + 5s to prevent premature disconnect during long waits

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Known Stubs
None - all BrowserCommand variants are fully wired to JSON-RPC dispatch.

## Next Phase Readiness
- CLI sends surface_ref in params; Plan 02 (socket-side dispatch) can resolve surface_ref to UUID
- All 23 browser subcommands compile and dispatch; socket handlers need matching implementations

---
*Phase: 12.1-cli-browser-parity-skill-packaging*
*Completed: 2026-03-30*
