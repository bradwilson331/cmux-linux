---
phase: 01-ghostty-foundation
plan: 07
type: execute
wave: 1
depends_on: []
files_modified: [src/ghostty/surface.rs, src/main.rs]
autonomous: true
gap_closure: true
requirements: [GHOST-02, GHOST-03]
must_haves:
  truths:
    - "App starts without crashing"
    - "Ghostty surface is properly initialized"
    - "Terminal window appears and accepts input"
  artifacts:
    - path: "src/ghostty/surface.rs"
      provides: "Fixed surface initialization"
      min_lines: 380
    - path: "src/main.rs"
      provides: "Proper app lifecycle"
      min_lines: 80
  key_links:
    - from: "src/main.rs"
      to: "ghostty::surface::create_surface"
      via: "build_ui function"
---

<objective>
Fix the crash on startup by ensuring proper Ghostty initialization order

Purpose: Enable the app to start and display a terminal window
Output: Running app with visible terminal
</objective>

<context>
@.planning/phases/01-ghostty-foundation/01-VERIFICATION.md
@src/ghostty/surface.rs
@src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Ghostty initialization timing</name>
  <files>src/ghostty/surface.rs</files>
  <action>
    Refactor ghostty initialization to ensure proper lifecycle:
    - Move ghostty_init() call to happen AFTER GLArea is realized (GL context must exist)
    - Add null checks before using surface pointer
    - Ensure GL context is made current before any ghostty GL operations
    - Add debug logging to trace initialization sequence
    
    The crash in ghostty_surface_set_content_scale likely happens because:
    1. Surface is created before GL context exists
    2. Or the surface pointer is invalid/null
    
    Fix by deferring surface creation to the realize callback when GL is ready.
  </action>
  <verify>cargo build succeeds without warnings</verify>
  <done>Surface initialization moved to proper lifecycle point</done>
</task>

<task type="auto">
  <name>Task 2: Add defensive checks and error handling</name>
  <files>src/ghostty/surface.rs, src/main.rs</files>
  <action>
    Add safety checks throughout the initialization path:
    - Check if GL context is current before ghostty operations
    - Validate surface pointer is non-null after creation
    - Add error handling for ghostty_surface_new failure
    - Print debug messages at each initialization step
    - Handle the case where ghostty_app_new or ghostty_surface_new returns null
    
    In main.rs:
    - Add error handling for window creation
    - Ensure proper cleanup on failure
  </action>
  <verify>App starts without segfault (may still have issues, but no crash)</verify>
  <done>Defensive programming prevents crashes during initialization</done>
</task>

</tasks>

<verification>
Run: timeout 2 ./target/debug/cmux-linux
Expected: App starts (may show errors but no segfault)
</verification>

<success_criteria>
- App launches without crashing
- Debug output shows initialization sequence
- Window appears (even if terminal not fully working)
</success_criteria>

<output>
After completion, create `.planning/phases/01-ghostty-foundation/01-07-SUMMARY.md`
</output>