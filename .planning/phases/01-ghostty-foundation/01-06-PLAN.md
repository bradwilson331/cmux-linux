---
phase: 01-ghostty-foundation
plan: 06
type: execute
wave: 6
depends_on: [01-05]
files_modified: [src/main.rs, Cargo.toml, src/lib.rs, src/ghostty/mod.rs]
autonomous: true
requirements: [GHOST-07]
user_setup: []
gap_closure: true

must_haves:
  truths:
    - "Tokio runtime is initialized and running"
    - "GLib main loop and tokio runtime coexist"
    - "glib::MainContext::channel bridges tokio to GTK main thread"
  artifacts:
    - path: "src/main.rs"
      provides: "Tokio runtime initialization"
      contains: "tokio::runtime"
    - path: "Cargo.toml"
      provides: "Tokio dependency"
      contains: 'tokio = { version'
  key_links:
    - from: "tokio runtime"
      to: "GLib main loop"
      via: "glib::MainContext::channel"
      pattern: "MainContext::channel"
---

<objective>
Add tokio runtime to meet Phase 1 success criteria for async foundation

Purpose: Close verification gap where success criteria mentions tokio+GLib but only GLib exists
Output: Application with both tokio runtime and GLib main loop properly bridged
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ghostty-foundation/01-VERIFICATION.md
@.planning/phases/01-ghostty-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tokio dependency and runtime setup</name>
  <files>Cargo.toml, src/main.rs</files>
  <action>
    Add tokio runtime to satisfy Phase 1 success criteria #4:
    
    In Cargo.toml:
    - Add tokio dependency with features = ["full"]
    
    In src/main.rs:
    - Create tokio runtime before GTK app initialization
    - Use tokio::runtime::Runtime::new() or Runtime::new_multi_thread()
    - Spawn the runtime in a separate thread to avoid blocking GTK
    - Set up glib::MainContext::channel() to bridge tokio tasks to GTK main thread
    
    Note from CONTEXT.md D-07: Originally deferred to Phase 3, but ROADMAP success
    criteria explicitly requires it in Phase 1 for foundation verification.
    
    Gap reason: Success criteria mentions tokio but not used
  </action>
  <verify>
    <automated>cargo build && grep -q "tokio" Cargo.toml && grep -q "Runtime::new" src/main.rs</automated>
  </verify>
  <done>Tokio runtime created and running alongside GLib main loop</done>
</task>

<task type="auto">
  <name>Task 2: Implement tokio-GLib bridge pattern</name>
  <files>src/main.rs, src/lib.rs</files>
  <action>
    Implement the glib::MainContext::channel bridge pattern:
    
    - Create a channel using glib::MainContext::channel(Priority::default())
    - Store the sender in a way accessible to tokio tasks
    - Set up receiver to process messages in GTK main thread
    - Add a simple test message to verify the bridge works
    
    This establishes the foundation for Phase 3 socket server integration.
    The pattern should allow tokio tasks to send messages that execute on
    the GTK main thread without blocking.
    
    If lib.rs was removed in plan 05, adjust implementation accordingly.
    
    Reference from STATE.md: tokio for socket I/O; glib::MainContext::channel bridges tokio to GTK main thread
  </action>
  <verify>
    <automated>cargo test 2>/dev/null | grep -q "test result" || cargo build</automated>
  </verify>
  <done>glib::MainContext::channel successfully bridges tokio to GTK</done>
</task>

<task type="auto">
  <name>Task 3: Verify runtime coexistence</name>
  <files>src/main.rs</files>
  <action>
    Add minimal verification that both runtimes work together:
    
    - Add a tokio::spawn() task that runs after app start
    - Have it send a test message through the glib channel
    - Log or print confirmation when message is received on GTK side
    - Ensure neither runtime blocks the other
    
    This verifies the success criteria: "tokio runtime started, GLib main loop runs,
    glib::MainContext::channel bridges the two"
    
    Gap reason: Need to verify both runtimes coexist properly
  </action>
  <verify>
    <automated>timeout 2 cargo run 2>&1 | grep -E "(tokio|glib|GTK)" | head -1</automated>
  </verify>
  <done>Both tokio and GLib runtimes initialize and run without conflict</done>
</task>

</tasks>

<verification>
After completion:
- cargo build succeeds with tokio dependency
- Application starts both tokio runtime and GLib main loop
- Test message successfully crosses from tokio to GTK thread
</verification>

<success_criteria>
- Tokio runtime initializes before GTK app
- GLib main loop runs without blocking
- glib::MainContext::channel successfully bridges the two
- App meets Phase 1 success criteria #4
</success_criteria>

<output>
After completion, create `.planning/phases/01-ghostty-foundation/01-06-SUMMARY.md`
</output>