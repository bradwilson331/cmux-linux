---
phase: 04-notifications-hidpi-ssh
plan: 05
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
  - "04-03"
  - "04-04"
files_modified: []
autonomous: false
requirements:
  - NOTF-01
  - NOTF-02
  - NOTF-03
  - HDPI-01
  - HDPI-02
  - SSH-01
  - SSH-02
  - SSH-03
  - SSH-04
must_haves:
  truths:
    - "All Phase 4 features verified by human"
    - "Bell attention, sidebar dot, desktop notification all work end-to-end"
    - "HiDPI rendering verified at multiple scales"
    - "SSH workspace creation and connection verified (or limitations documented)"
---

<objective>
Human verification of all Phase 4 features: notifications, HiDPI, and SSH workspaces.

Purpose: Final checkpoint ensuring all features work correctly in the running application before marking Phase 4 complete.
Output: Verified Phase 4 feature set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-notifications-hidpi-ssh/04-CONTEXT.md
@.planning/phases/04-notifications-hidpi-ssh/04-01-SUMMARY.md
@.planning/phases/04-notifications-hidpi-ssh/04-02-SUMMARY.md
@.planning/phases/04-notifications-hidpi-ssh/04-03-SUMMARY.md
@.planning/phases/04-notifications-hidpi-ssh/04-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify Phase 4 features</name>
  <what-built>
    Phase 4 implementation:
    1. Bell attention tracking (action_cb RING_BELL -> per-pane has_attention -> workspace dot)
    2. Desktop notifications via GNotification when window unfocused
    3. notification.list and notification.clear socket commands
    4. HiDPI verification and any fixes
    5. SSH remote workspaces via socket API (workspace.create with remote_target)
    6. SSH connection state sidebar indicator
    7. Exponential backoff reconnection
  </what-built>
  <how-to-verify>
    **Notifications (NOTF-01, NOTF-02, NOTF-03):**
    1. Open app, create two workspaces
    2. In workspace 2, run: `echo -e '\a'` (terminal bell)
    3. Switch to workspace 1 -- verify amber dot appears next to "Workspace 2" in sidebar
    4. Switch to workspace 2 -- verify dot disappears (attention cleared)
    5. Switch to workspace 1, then switch to another window (unfocus the app)
    6. In a separate terminal, use socket API to send bell: run `echo -e '\a'` in workspace 2
    7. Verify desktop notification appears: "Terminal Bell" / "Workspace 2 - Terminal bell"
    8. Verify rapid bells don't flood notifications (5-second rate limit)

    **Socket API (notification.*):**
    9. Use socket CLI: `echo '{"id":1,"method":"notification.list"}' | socat - UNIX-CONNECT:$XDG_RUNTIME_DIR/cmux/cmux.sock`
    10. Verify response includes workspace attention state
    11. Test notification.clear with workspace UUID

    **HiDPI (HDPI-01, HDPI-02):**
    12. If multi-DPI monitors available: drag window between monitors, verify text stays crisp
    13. Verify sidebar text, attention dot, and dividers scale correctly
    14. Check debug log for scale-factor change messages

    **SSH (SSH-01 through SSH-04) — if SSH target available:**
    15. Via socket: `{"id":1,"method":"workspace.create","params":{"remote_target":"user@host"}}`
    16. Verify sidebar shows SSH workspace with "Reconnecting..." then "Connected" subtitle
    17. Verify terminal in SSH workspace runs on remote host
    18. Disconnect network briefly, verify "Disconnected" state, then reconnection
    19. Close SSH workspace, verify no zombie SSH processes

    **If SSH target not available:** Document that SSH code compiled and socket API accepts remote_target parameter. Manual SSH testing deferred to integration environment.
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 4, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
Human verification of all Phase 4 success criteria from ROADMAP.md.
</verification>

<success_criteria>
All Phase 4 features verified working by human. Any issues documented and addressed or tracked for gap closure.
</success_criteria>

<output>
After completion, create `.planning/phases/04-notifications-hidpi-ssh/04-05-SUMMARY.md`
</output>
