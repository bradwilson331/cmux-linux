---
phase: 11-desktop-integration-dependency-detection
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packaging/scripts/detect-deps.sh
autonomous: true
requirements:
  - BUILD-02

must_haves:
  truths:
    - "Running detect-deps.sh on cmux-app binary produces a table of shared libraries mapped to Debian and Fedora package names"
    - "GTK4, fontconfig, freetype, and oniguruma appear in the output"
    - "Script works on Debian/Ubuntu host and provides Fedora names via fallback table"
  artifacts:
    - path: "packaging/scripts/detect-deps.sh"
      provides: "Dependency detection script mapping ldd output to distro package names"
      contains: "ldd"
  key_links:
    - from: "packaging/scripts/detect-deps.sh"
      to: "cmux-app binary"
      via: "ldd $BINARY to discover shared library dependencies"
      pattern: "ldd.*BINARY"
---

<objective>
Create a dependency detection script that maps shared library dependencies of the cmux-app binary to both Debian and Fedora package names.

Purpose: Automate discovery of runtime dependencies so packaging phases (12-14) declare correct package dependencies without manual lookup.
Output: An executable bash script that produces human-readable and machine-parseable dependency mappings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-desktop-integration-dependency-detection/11-CONTEXT.md
@.planning/phases/11-desktop-integration-dependency-detection/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dependency detection script</name>
  <files>packaging/scripts/detect-deps.sh</files>
  <read_first>
    .planning/phases/11-desktop-integration-dependency-detection/11-RESEARCH.md
  </read_first>
  <action>
Create `packaging/scripts/detect-deps.sh` with the following implementation. Per D-09: bash script using ldd + dpkg -S / rpm -qf. Per D-10: always outputs both Debian and Fedora package names; native package manager when available, static fallback table for the other.

```bash
#!/usr/bin/env bash
set -euo pipefail

# detect-deps.sh -- Map runtime shared library dependencies to distro package names
# Usage: ./detect-deps.sh <binary>
# Output: TSV table (library, debian_pkg, fedora_pkg) + optional JSON (--json flag)

BINARY="${1:?Usage: detect-deps.sh [--json] <binary>}"
JSON_MODE=false

if [[ "$1" == "--json" ]]; then
    JSON_MODE=true
    BINARY="${2:?Usage: detect-deps.sh [--json] <binary>}"
fi

if [[ ! -f "$BINARY" ]]; then
    echo "ERROR: Binary not found: $BINARY" >&2
    exit 1
fi

# Static fallback: lib soname -> Fedora package name
# Used when rpm is not available (e.g., running on Debian/Ubuntu)
declare -A FEDORA_FALLBACK=(
    ["libgtk-4.so.1"]="gtk4"
    ["libfontconfig.so.1"]="fontconfig"
    ["libfreetype.so.6"]="freetype"
    ["libonig.so.5"]="oniguruma"
    ["libGL.so.1"]="mesa-libGL"
    ["libEGL.so.1"]="mesa-libEGL"
    ["libharfbuzz.so.0"]="harfbuzz"
    ["libgio-2.0.so.0"]="glib2"
    ["libgobject-2.0.so.0"]="glib2"
    ["libglib-2.0.so.0"]="glib2"
    ["libcairo.so.2"]="cairo"
    ["libcairo-gobject.so.2"]="cairo-gobject"
    ["libpango-1.0.so.0"]="pango"
    ["libpangocairo-1.0.so.0"]="pango"
    ["libpangoft2-1.0.so.0"]="pango"
    ["libgdk_pixbuf-2.0.so.0"]="gdk-pixbuf2"
    ["libepoxy.so.0"]="libepoxy"
    ["libX11.so.6"]="libX11"
    ["libXi.so.6"]="libXi"
    ["libXrandr.so.2"]="libXrandr"
    ["libXcursor.so.1"]="libXcursor"
    ["libwayland-client.so.0"]="wayland-devel"
    ["libxkbcommon.so.0"]="libxkbcommon"
    ["libgraphene-1.0.so.0"]="graphene"
    ["libvulkan.so.1"]="vulkan-loader"
    ["libfribidi.so.0"]="fribidi"
    ["libpixman-1.so.0"]="pixman"
    ["libpng16.so.16"]="libpng"
    ["libz.so.1"]="zlib"
)

# Static fallback: lib soname -> Debian package name
# Used when dpkg is not available (e.g., running on Fedora)
declare -A DEBIAN_FALLBACK=(
    ["libgtk-4.so.1"]="libgtk-4-1"
    ["libfontconfig.so.1"]="libfontconfig1"
    ["libfreetype.so.6"]="libfreetype6"
    ["libonig.so.5"]="libonig5"
    ["libGL.so.1"]="libgl1"
    ["libEGL.so.1"]="libegl1"
    ["libharfbuzz.so.0"]="libharfbuzz0b"
    ["libgio-2.0.so.0"]="libglib2.0-0"
    ["libgobject-2.0.so.0"]="libglib2.0-0"
    ["libglib-2.0.so.0"]="libglib2.0-0"
    ["libcairo.so.2"]="libcairo2"
    ["libcairo-gobject.so.2"]="libcairo-gobject2"
    ["libpango-1.0.so.0"]="libpango-1.0-0"
    ["libpangocairo-1.0.so.0"]="libpangocairo-1.0-0"
    ["libpangoft2-1.0.so.0"]="libpangoft2-1.0-0"
    ["libgdk_pixbuf-2.0.so.0"]="libgdk-pixbuf-2.0-0"
    ["libepoxy.so.0"]="libepoxy0"
    ["libX11.so.6"]="libx11-6"
    ["libxkbcommon.so.0"]="libxkbcommon0"
    ["libgraphene-1.0.so.0"]="libgraphene-1.0-0"
    ["libvulkan.so.1"]="libvulkan1"
    ["libfribidi.so.0"]="libfribidi0"
    ["libpng16.so.16"]="libpng16-16"
    ["libz.so.1"]="zlib1g"
)

# System libs to skip (always present, not package dependencies)
SKIP_PATTERN='^(libc\.so|libm\.so|libpthread|libdl|librt\.so|linux-vdso|ld-linux|libgcc_s|libstdc\+\+)'

HAS_DPKG=false
HAS_RPM=false
command -v dpkg &>/dev/null && HAS_DPKG=true
command -v rpm &>/dev/null && HAS_RPM=true

# Collect unique library sonames from ldd
mapfile -t LIBS < <(ldd "$BINARY" 2>/dev/null | awk '/=>/ {print $1}' | grep -v -E "$SKIP_PATTERN" | sort -u)

if $JSON_MODE; then
    echo "["
fi

FIRST=true
for lib in "${LIBS[@]}"; do
    # Debian package resolution
    if $HAS_DPKG; then
        deb_pkg=$(dpkg -S "*/$lib" 2>/dev/null | head -1 | cut -d: -f1) || true
        deb_pkg="${deb_pkg:-${DEBIAN_FALLBACK[$lib]:-UNKNOWN}}"
    else
        deb_pkg="${DEBIAN_FALLBACK[$lib]:-UNKNOWN}"
    fi

    # Fedora package resolution
    if $HAS_RPM; then
        lib_path=$(ldconfig -p 2>/dev/null | grep "$lib" | awk '{print $NF}' | head -1) || true
        if [[ -n "$lib_path" ]]; then
            fed_pkg=$(rpm -qf "$lib_path" 2>/dev/null | head -1) || true
            fed_pkg="${fed_pkg:-${FEDORA_FALLBACK[$lib]:-UNKNOWN}}"
        else
            fed_pkg="${FEDORA_FALLBACK[$lib]:-UNKNOWN}"
        fi
    else
        fed_pkg="${FEDORA_FALLBACK[$lib]:-UNKNOWN}"
    fi

    if $JSON_MODE; then
        $FIRST || echo ","
        FIRST=false
        printf '  {"library": "%s", "debian": "%s", "fedora": "%s"}' "$lib" "$deb_pkg" "$fed_pkg"
    else
        printf '%s\t%s\t%s\n' "$lib" "$deb_pkg" "$fed_pkg"
    fi
done

if $JSON_MODE; then
    echo ""
    echo "]"
else
    # Summary: unique package names
    echo ""
    echo "--- Debian packages (unique) ---"
    ldd "$BINARY" 2>/dev/null | awk '/=>/ {print $1}' | grep -v -E "$SKIP_PATTERN" | sort -u | while read -r lib; do
        if $HAS_DPKG; then
            dpkg -S "*/$lib" 2>/dev/null | head -1 | cut -d: -f1
        else
            echo "${DEBIAN_FALLBACK[$lib]:-UNKNOWN}"
        fi
    done | sort -u

    echo ""
    echo "--- Fedora packages (unique) ---"
    ldd "$BINARY" 2>/dev/null | awk '/=>/ {print $1}' | grep -v -E "$SKIP_PATTERN" | sort -u | while read -r lib; do
        echo "${FEDORA_FALLBACK[$lib]:-UNKNOWN}"
    done | sort -u
fi
```

Make executable: `chmod +x packaging/scripts/detect-deps.sh`

Test the script against the cmux-app binary. If the binary is not yet built at `target/debug/cmux-app`, build it first: `cargo build --bin cmux-app`.

Run: `bash packaging/scripts/detect-deps.sh target/debug/cmux-app`

Verify that the output includes entries for libgtk-4, libfontconfig, libfreetype, and libonig.

Also test JSON mode: `bash packaging/scripts/detect-deps.sh --json target/debug/cmux-app | head -5`
  </action>
  <verify>
    <automated>cd /home/twilson/code/cmux-linux && cargo build --bin cmux-app 2>/dev/null; bash packaging/scripts/detect-deps.sh target/debug/cmux-app 2>&1 | grep -q "libgtk" && echo "Dependency detection works"</automated>
  </verify>
  <acceptance_criteria>
    - packaging/scripts/detect-deps.sh is executable (test -x)
    - packaging/scripts/detect-deps.sh contains `ldd "$BINARY"`
    - packaging/scripts/detect-deps.sh contains `FEDORA_FALLBACK` associative array
    - packaging/scripts/detect-deps.sh contains `DEBIAN_FALLBACK` associative array
    - Running `detect-deps.sh target/debug/cmux-app` produces output containing "libgtk-4"
    - Running `detect-deps.sh target/debug/cmux-app` produces output containing "libfontconfig"
    - Running `detect-deps.sh --json target/debug/cmux-app` produces valid JSON array
    - Script exits 1 with usage message when called with no arguments
  </acceptance_criteria>
  <done>Dependency detection script maps ldd output to both Debian and Fedora package names. Human-readable TSV and JSON output modes work. GTK4, fontconfig, freetype, and oniguruma dependencies are detected.</done>
</task>

</tasks>

<verification>
- `bash packaging/scripts/detect-deps.sh target/debug/cmux-app | grep libgtk` shows GTK4 dependency mapped
- `bash packaging/scripts/detect-deps.sh --json target/debug/cmux-app | python3 -m json.tool` validates JSON
- Script output includes both Debian and Fedora columns
- Running with no args exits non-zero
</verification>

<success_criteria>
1. detect-deps.sh maps ldd shared library output to Debian and Fedora package names
2. Output includes key dependencies: GTK4, fontconfig, freetype, oniguruma
3. Both human-readable (TSV) and machine-parseable (JSON) output modes work
4. Script works on Debian/Ubuntu host using dpkg natively and Fedora fallback table
5. System libraries (libc, libm, ld-linux) are filtered out
</success_criteria>

<output>
After completion, create `.planning/phases/11-desktop-integration-dependency-detection/11-03-SUMMARY.md`
</output>
