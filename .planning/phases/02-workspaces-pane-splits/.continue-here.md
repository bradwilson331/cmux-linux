---
phase: 02-workspaces-pane-splits
task: 1
total_tasks: 1
status: blocked_on_human_testing
last_updated: 2026-03-25T04:26:53.758Z
---

<current_state>
Plan 02-06 (human verification checkpoint) is the only remaining plan. UAT steps 1-4 passed. Step 5 (Ctrl+D split right) failed — cursor froze after the pane resize triggered by the split. Two fixes were applied this session. Build passes. Awaiting UAT re-test.
</current_state>

<completed_work>
- Plans 02-00 through 02-05: All implementation complete
- Plan 02-07: Gap closure (drag freeze, pane-close crash, wrong keybinds) — done
- Plan 02-08: Gap closure (unified focus/render regression, 4 bugs) — done
- UAT steps 1-4: Approved (initial state, workspace create/switch/rename)
- Fix applied this session (NOT yet in a named plan):
  - src/ghostty/surface.rs: removed set_focus(true) from re-realize path
  - src/ghostty/surface.rs: moved cell storage before grab_focus() in fresh realize
</completed_work>

<remaining_work>
- Plan 02-06: Human UAT — re-test steps 5-12 with latest build
  - Step 5: Ctrl+D split right — was failing (cursor freeze after resize)
  - Step 7: Pane resize (divider drag) — not yet tested
  - Steps 8-12: Not yet tested
</remaining_work>

<decisions_made>
- Removed set_focus(true) from re-realize: During split, old pane (start child of GtkPaned) was re-realized and incorrectly got set_focus(true), causing both panes focused=true. Subsequent EventControllerFocus leave on new pane killed cursor blink. Now only EventControllerFocus enter handles restoration.
- Cell stored before grab_focus: EventControllerFocus enter was no-op (cell=None) because surface wasn't stored until after grab_focus. Fixed order.
</decisions_made>

<blockers>
- Cursor freeze on split (and possibly pane resize) — fix applied, awaiting verification
- If window resize STILL freezes after fix: separate root cause. Next step = add eprintln! to EventControllerFocus enter/leave handlers, run app, split pane, share log output.
</blockers>

<context>
Two-level early-return guard issue: Ghostty has one guard in Surface.zig (self.focused == focused) and another in Thread.zig renderer (self.flags.focused == v). The re-realize calling set_focus(old,true) was corrupting this state chain. The new EventControllerFocus approach is cleaner: focus only changes when GTK actually routes keyboard focus to/from the widget.
</context>

<next_action>
cargo build && ./target/debug/cmux-linux — re-test UAT steps 5-12. If all pass: type "approved" for plan 02-06, then run /gsd:execute-phase 02 to trigger verification and complete the phase.
</next_action>
